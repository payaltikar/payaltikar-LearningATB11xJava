package ex_01_Java_Basic;

public class Can_I_create_programme_without_main {

    // Main() method is the entry pt. of standalone java application so it is always impt to create it
    // Anyways it depends on the versions which one u r using on that
    //    JDK< 7 --> yes (Using static block)
    //    JDK 8 to 21 --> No
    //    JDK > 21 --> yes ( if static)
}
